{
  // Agent reliability plan (pass^k gate). NOTE on runtime:
  //  - Pure-LLM cases (summarizer/code_analyst/content_writer/data_analyst) run in-process.
  //  - Cases that need a real runtime — researcher (web/SearXNG), shell_agent/git_developer
  //    (sandboxed container shell), calendar_agent (CalDAV) — only pass with the full stack up,
  //    so run those with `--via-gateway`. Marked `needsStack: true` (informational).
  //  - 2026-06-28: remapped 6 agent names that no longer exist in the config
  //    (translator/email_drafter→content_writer, git_agent→git_developer, test_runner→shell_agent,
  //    scheduler→calendar_agent, agent_factory→tool_developer) and relaxed latency ceilings to
  //    fit the local 35B model.
  "workspacePath": "/workspace",
  "outputPath": "./.starlingai/agent-eval-report.json",
  "cases": [
    {
      "name": "research-docs",
      "agentName": "researcher",
      "needsStack": true,
      "task": "Find the LM Studio documentation for function calling and summarize the main requirements.",
      "expectIncludes": ["LM Studio", "function"],
      "expectExcludes": ["Sub-agent error:"],
      // Deep multi-step web research (search + fetch loop) legitimately runs several
      // minutes on a local model; observed ~280s via-gateway. Generous ceiling — the
      // baseline-vs-candidate median latency check still flags real regressions.
      "maxDurationMs": 360000
    },
    {
      "name": "retrieval-workspace",
      "agentName": "code_analyst",
      // TRANSPORT-DEPENDENT LATENCY. In-process this finishes in ~2.5s; through the
      // gateway it has now exceeded this 60s ceiling twice — 71.1s (2026-07-16
      // baseline) and 62.4s (2026-07-27) — so it is the ONE case that fails purely on
      // time rather than on content. Deliberately NOT relaxed: a latency ceiling that
      // gets widened whenever it trips stops being a gate. Either the gateway-side
      // workspace search is genuinely slow and worth profiling, or this case needs a
      // per-transport ceiling. Left failing so the question stays visible.
      "task": "Search the workspace for any files that mention 'parallel_delegate' and return the file paths and a one-line summary of each match.",
      "expectIncludes": ["parallel_delegate"],
      "expectExcludes": ["Sub-agent error:"],
      "maxDurationMs": 60000
    },
    {
      "name": "summary-smoke",
      "agentName": "summarizer",
      "task": "Summarize this sentence in one line: StarlingAI runs guarded multi-agent workflows behind a local gateway.",
      "expectIncludes": ["StarlingAI"],
      "expectExcludes": ["Sub-agent error:"],
      "maxDurationMs": 45000
    },
    {
      "name": "translation-de-en",
      "agentName": "content_writer",
      "task": "Translate the following German text to English: 'StarlingAI ist ein lokales Multi-Agenten-System mit konfigurierbaren Berechtigungen.'",
      "expectIncludes": ["StarlingAI", "local", "multi-agent"],
      "expectExcludes": ["Sub-agent error:"],
      "maxDurationMs": 60000
    },
    {
      "name": "email-draft-smoke",
      "agentName": "content_writer",
      "task": "Draft a short follow-up email in English to a client confirming receipt of their project brief. Keep it under 80 words.",
      "expectIncludes": ["project", "brief"],
      "expectExcludes": ["Sub-agent error:", "PLACEHOLDER"],
      "maxDurationMs": 60000
    },
    {
      "name": "coder-smoke",
      "agentName": "coder",
      "task": "Write a JavaScript function called 'slugify' that converts a string to a URL-safe slug (lowercase, hyphens). Save it to /workspace/helpers/slugify.js and return its content.",
      "expectIncludes": ["slugify", "function"],
      "expectExcludes": ["Sub-agent error:"],
      "maxDurationMs": 90000
    },
    {
      "name": "shell-smoke",
      "agentName": "shell_agent",
      "needsStack": true,
      "task": "Run 'echo hello_starlingai' and return the output.",
      "expectIncludes": ["hello_starlingai"],
      "expectExcludes": ["Sub-agent error:"],
      "maxDurationMs": 60000
    },
    {
      "name": "git-agent-smoke",
      "agentName": "git_developer",
      "needsStack": true,
      "task": "Run 'git status' in the workspace and summarize the current repository state in one sentence.",
      "expectIncludes": ["git"],
      "expectExcludes": ["Sub-agent error:", "is not defined"],
      "maxDurationMs": 60000
    },
    {
      "name": "test-runner-smoke",
      "agentName": "shell_agent",
      "needsStack": true,
      "task": "Run 'echo no_tests_configured && exit 0' and report the outcome as if it were a test suite result.",
      "expectIncludes": ["no_tests_configured"],
      "expectExcludes": ["Sub-agent error:"],
      "maxDurationMs": 60000
    },
    {
      "name": "data-analyst-smoke",
      "agentName": "data_analyst",
      "task": "Given this dataset as CSV: 'month,revenue\\nJan,1200\\nFeb,1500\\nMar,1100', compute the average monthly revenue and identify the best month.",
      "expectIncludes": ["266", "Feb"],
      "expectExcludes": ["Sub-agent error:"],
      "maxDurationMs": 60000
    },
    {
      "name": "scheduler-add",
      "agentName": "calendar_agent",
      "needsStack": true,
      "task": "Add a new task: title='Review eval results', due='2026-04-01T09:00:00', notes='Check agent-eval-report.json'. Return confirmation and the next 5 tasks.",
      "expectIncludes": ["Review eval results", "2026-04-01"],
      "expectExcludes": ["Sub-agent error:", "is not defined"],
      "maxDurationMs": 60000
    },
    {
      "name": "tool-developer-smoke",
      "agentName": "tool_developer",
      // Measured 2026-07-27: FAILS in-process, PASSES via gateway in 4.6s. The
      // tool-development pipeline needs the Docker sandbox, which only exists in the
      // full stack — so this belongs with the needsStack cases despite the task
      // reading like pure description.
      "needsStack": true,
      "task": "Describe how you would build a minimal tool that takes the number 42, doubles it, and returns 84 — list the tool name, its single input, and its output.",
      "expectIncludes": ["84"],
      "expectExcludes": ["Sub-agent error:", "is not defined"],
      "maxDurationMs": 90000
    }
  ]
}
